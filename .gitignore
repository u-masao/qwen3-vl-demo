# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs / uv
.venv/
.uv/

# Project artifacts (generated data, trained models, metrics)
data/
data_smoke/
outputs/
outputs_smoke/

# Issue #15 gamma スイープ検証の生データ（解析用に手元保持・PR には含めない）
verify_issue15/

# HF caches that might land in-tree
.cache/

# MLflow experiment tracking (SQLite backend + <repo>/mlflow/<exp_id> artifacts)
mlflow.db
mlflow.db-*
mlflow/
mlruns/

# Claude Code (local, per-developer)
CLAUDE.md

# Editor / OS
.idea/
.vscode/
.DS_Store

*.sw[po]
nohup.out
